{
  "name": "chainbrary",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ chainbrary",
    "create-local": "graph create --node http://localhost:8020/ chainbrary",
    "remove-local": "graph remove --node http://localhost:8020/ chainbrary",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chainbrary",
    "test": "graph test",
    "prettier": "prettier --write 'contracts/*.sol'"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.6",
    "@graphprotocol/graph-ts": "0.29.1",
    "@openzeppelin/contracts": "^4.8.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "prettier": "^2.8.3",
    "prettier-plugin-solidity": "^1.1.1"
  }
}
